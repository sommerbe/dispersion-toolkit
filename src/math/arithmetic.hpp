#pragma once
#include "types.hpp"

namespace dptk {
namespace math {

b64 round(b64 v);
// b128 round(b128 v);

b64 min(b64 a, b64 b);
b64 max(b64 a, b64 b);
b64 abs(b64 a);

} // namespace math
} // namespace dptk
